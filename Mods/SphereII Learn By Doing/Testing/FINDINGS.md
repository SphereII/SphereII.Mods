# LBD static audit - findings

_Generated by `Testing/lbd_audit.py`. 119 XML files, 239 tuning cvars, 80 progressions, 230 award paths._

## 1. Award paths that can never pay out

The value expression resolves to no cvar, and an unset cvar reads as 0, so the trigger fires and grants nothing.

| progression | trigger | value | file |
|---|---|---|---|

## 2. Progressions with no working XP path at all

_(none)_

## 3. Progression names with no vanilla definition

_(none)_

## 3b. progression_name references the game does not know

Checked against vanilla `progression.xml` across every site - level-up, decay and requirements. An unknown name is inert: `AddProgressionLevel` does nothing and `ProgressionLevel` reads 0.

_(none)_

## 3c. progression_name casing that differs from vanilla

Lower severity - inert only if the game's progression lookup is case-sensitive. Worth normalising either way.

_(none)_

## 4. Throttle buffs referenced but never defined

`NotHasBuff` against a buff that does not exist is always true, so these award paths have no cooldown and score on every trigger.

_(none)_

## 4b. Buff references that do not resolve

Buff names are case sensitive. `AddBuff` against an unresolvable name is a silent no-op - a `_LevelUpCheck` listed here means that progression can never level. `NotHasBuff` against one is always true, removing the throttle.

_(none)_

## 4c. Block requirements that match no block

Tags and Harvest drops are resolved through the Extends chain, the way BlocksFromXml inherits them - a block with no Tags of its own carries its parent's. A requirement matching nothing is a silently dead path.

_(none - all 12 distinct block requirements match at least one block)_

## 4d. SCore requirement and action names that resolve to no class

A requirement names its class exactly; an action names its class without the MinEventAction prefix. An unresolvable name is dropped silently - the effect still runs, just without that condition.

_(none - every SCore requirement and action resolves)_

## 4e. Debug lines that can never print

A log line gated on NotHasBuff of a cooldown must be ordered BEFORE the AddBuff that applies it. Where it is not, the award still pays but the line is silently suppressed, so the progression looks dead in the log.

_(none)_

## 5. Runtime observability gaps

All debug output is gated behind `HasBuff god`, so `buff god` enables the whole stream. These progressions still cannot be fully followed in the log:

| progression | silent award paths | logs level-up check | logs level-up success |
|---|---|---|---|
| `attAgility` | 0/6 | NO | yes |
| `attFortitude` | 0/8 | NO | yes |
| `attIntellect` | 0/9 | NO | yes |
| `attPerception` | 0/9 | NO | yes |
| `perkDemolitionsExpert` | 0/2 | NO | yes |
| `perkFlurryOfStrength` | 0/2 | NO | yes |
| `perkFlurryOfFortitude` | 0/2 | NO | yes |
| `perkFlurryOfPerception` | 0/2 | NO | yes |
| `perkGrandSlam` | 0/1 | NO | yes |
| `perkInfiltrator` | 0/2 | NO | yes |
| `perkJavelinMaster` | 0/2 | NO | yes |
| `perkJunkMiner` | 0/2 | NO | yes |
| `perkMiner69r` | 0/2 | NO | yes |
| `perkMotherLode` | 0/2 | NO | yes |
| `perkPackMule` | 0/6 | NO | yes |
| `perkPummelPete` | 0/2 | NO | yes |
| `perkRuleOneCardio` | 0/2 | NO | yes |
| `perkSalvageOperations` | 0/2 | NO | yes |
| `perkSkullCrusher` | 0/2 | NO | yes |
| `perkSlowMetabolism` | 0/4 | NO | yes |

Install the generated `SphereII Learn By Doing Test Harness` modlet to close these gaps.
